#include <stdio.h>

/**
 * main - Entry poimt
 * Return: Always 0 (success)
 */
int main(void)
{
	char letter;

	for (letter = 'a'; c <= 'z'; c++)
	{
		if (c != 'e'; &&c != 'q')
		{
			putchar(c);
		}
	}
	putchar('\n');
	return (0);
}

	        	    
